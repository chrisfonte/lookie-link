# Lookout Configuration
# ---------------------
# Copy this to lookout.yaml and customize for your setup.
# After editing, restart the server for changes to take effect.

# Server settings
server:
  port: 9876
  hostname: localhost  # e.g., my-server.local or my-host.tailnet.ts.net

# Repository mappings
# Each key becomes a URL prefix: /view/<key>/<path>
# Values are absolute paths or ~-relative paths to local directories.
repositories:
  docs: ~/Documents/docs
  notes: ~/notes
  # project: ~/projects/my-project

# Future settings (not yet implemented)
# search:
#   enabled: false
# auth:
#   enabled: false
# theme:
#   default: dark
